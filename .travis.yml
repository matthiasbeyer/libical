matrix:
  include:
    - language: nix
      script:
        - nix-shell --pure --run "cargo build --all --all-features"

jobs:
  fast_finish: true

notifications:
  email:
    on_success: never
    on_failure: always

